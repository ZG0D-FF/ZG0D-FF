{
  "buildCommand": "cd imposter-game && npm install && npm run build",
  "outputDirectory": ".",
  "cleanUrls": true,
  "crons": [{
    "path": "/api/jarvis-cron",
    "schedule": "0 0 * * *"
  }]
}
